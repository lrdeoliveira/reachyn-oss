import { Calendario } from "@/components/Calendario";

// 🗓️ CALENDÁRIO — F5 da Fábrica de Conteúdo: calendário editorial de 30 dias + séries recorrentes.
// Recorrência sem esforço — cada dia/episódio vira um vídeo pré-preenchido no Estúdio.
export default function Page() {
  return <Calendario />;
}
