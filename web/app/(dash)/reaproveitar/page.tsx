import { Reaproveitar } from "@/components/Reaproveitar";

// ♻️ REAPROVEITAR — F6 da Fábrica de Conteúdo: transformação de formato (1 vira 10). Um conteúdo
// longo vira 5 Shorts + carrossel + thread + pin. Multiplica o alcance reusando o que já foi feito.
export default function Page() {
  return <Reaproveitar />;
}
