# AUD-016: registry oficial fixo do npm.
# Garante que `npm audit`/`npm ci` resolvam contra o registry público (e não um mirror
# interno que pode não ter o feed de advisories), tornando a auditoria de dependências
# reproduzível na CI e no dev.
registry=https://registry.npmjs.org/
