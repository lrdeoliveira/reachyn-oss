; Overrides de produção do PHP (console). Carregado por último (zz-) em conf.d.
; max_execution_time alto: a pesquisa profunda síncrona pode levar minutos.
max_execution_time = 700
memory_limit = 256M
upload_max_filesize = 64M
post_max_size = 64M
expose_php = Off
